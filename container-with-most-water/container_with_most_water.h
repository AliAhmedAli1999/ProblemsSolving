/**
 * 
 * @param height A pointer to an array of integers where each element represents the height of a vertical line.
 * @param heightSize The number of elements in the height array.
 * 
 * @return The maximum area of water that can be trapped between any two lines in the height array.
 */
int maxArea(int* height, int heightSize);