#ifndef issubsequenceH
#define issubsequenceH

/*  @param s Pointer to the first character of the string `s`.
 *  @param t Pointer to the first character of the string `t`.
 *
 *  @return `true` if `s` is a subsequence of `t`, otherwise `false`.
 **/
bool isSubsequence(char* s, char* t);
#endif

